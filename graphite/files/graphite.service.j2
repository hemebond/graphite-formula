[Unit]
Description=Graphite
After=network.target

[Service]
User=_graphite
Group=_graphite
WorkingDirectory={{ graphite.paths.root }}
ExecStart=/usr/bin/uwsgi --ini {{ graphite.paths.conf }}/graphite.ini
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
